# Agent 协作协议

## Agent skills

### Issue tracker

项目使用 GitHub Issues；所有规格、决策票据和工作请求均发布到该仓库。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用 Matt Skills 默认的五类 triage 标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用 single-context 领域文档布局：根目录 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。
